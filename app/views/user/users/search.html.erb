<h4>検索結果</h4>
   <div class = "col-9">
       <table class = "table table-border">
        <thead>
          <tr>
           <th>名前</th>
           <th>専門種目</th>
           <th></th>
           <th></th>
           <th></th>
          </tr>
        </thead>
        <% @users.each do |user| %>
        <tbody>
         <tr>
          <td><%= user.name %></td>
          <td><%= user.event %></td>
          <td>フォロー<%= user.following_user.count %></td>
          <td>フォロワー<%= user.follower_user.count %></td>
          <td><%= link_to "ユーザー詳細ページ", user_path(user) %></td>
         </tr>
        </tbody>
        <% end %>
       </tbody>
   　 </div>